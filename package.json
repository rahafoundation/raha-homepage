{
  "name": "@raha/homepage",
  "version": "0.0.1",
  "description": "Google AMP static site for the Raha Foundation",
  "repository": "git@github.com:rahafoundation/raha-homepage.git",
  "author": "Omar Diab <me@omardiab.com>",
  "license": "Apache-2.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "static-i18n": "^0.1.24",
    "static-server": "^2.2.1"
  },
  "scripts": {
    "build": "./scripts/build",
    "deploy": "yarn build && gh-pages -d build/",
    "serve": "yarn build && cd build && static-server -o"
  }
}
